/**
 * Enum - Enumeration: Used to let other engineers know that this is a
 * collection of closely related values. We only use enums when we know all the
 * possible values while we are writing our code.
 */
export enum MatchResult {
    HomeWin = "H",
    AwayWin = "A",
    Draw = "D"
}
